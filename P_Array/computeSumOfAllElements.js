function computeSumOfAllElements(arr) {
	let word = [];
	if (str === '')
		return word;
	word = str.split(' ');
	return word
}


//빈 배열을 처음에 만들어 주고 만약 str이 빈 문자열 이라면
//빈문자열을 그대로 반환 해줍니다.
//빈문자가 아닐경우는 if문을 통과하여 밑에서 word에 str을 split하는데
//기준은 ' ' (공백)한칸으로 나누어서 담아줍니다.